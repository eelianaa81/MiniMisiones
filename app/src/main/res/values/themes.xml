<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.MiniMisiones" parent="android:Theme.Material.Light.NoActionBar" />

    <style name="Theme.MiniMisiones.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo_minimisiones</item>
        <item name="postSplashScreenTheme">@style/Theme.MiniMisiones</item>
    </style>

</resources>

